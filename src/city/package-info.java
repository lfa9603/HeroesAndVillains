/**
 * 
 */
/**
 * 
 * @author LorenzoFasano
 * This package contains: City.java a class that defines the overall structure of each level, 
 * CoordinatesTool.java which contains static methods
 * which help the construction of each City object and WorldBuilder.java which deals with the 
 * user input an creates an ArrayList of City objects (the  levels the player will have to 
 * complete to finish the game).
 *
 * It also contains a sub-package buildings which contains all objects (Building objects) needed to 
 * construct a city and the respective helper classes.  
 *
 */
package city;